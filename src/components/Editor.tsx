import React from 'react';

const Editor = ({ }) => {
    return (
        <>
            <div id='editor'>
                <textarea name="message" rows={10} cols={30}>
                </textarea>
            </div>
        </>
    );
}

export default Editor